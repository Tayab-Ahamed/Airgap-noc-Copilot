# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
scikit-learn==1.5.0

# Prediction engine
xgboost==2.0.3
prophet==1.1.5

# RAG / LLM (all run offline once models are pre-downloaded)
faiss-cpu==1.8.0
sentence-transformers==3.0.1
llama-index==0.10.50
langgraph==0.1.5
langchain==0.2.5
requests==2.32.3

# Parsing
textfsm==1.1.3

# Dev / test (optional at the venue; needed for `make test` and CI)
pytest==8.2.2
httpx==0.27.0

# Note: at an air-gapped venue, pre-download wheels:
#   pip download -r requirements.txt -d wheelhouse/
# then install with:
#   pip install --no-index --find-links wheelhouse/ -r requirements.txt
